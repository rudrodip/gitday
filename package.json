{
  "name": "gitday",
  "version": "1.0.3",
  "author": "",
  "main": "src/index.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/rudrodip/gitday.git"
  },
  "homepage": "https://github.com/rudrodip/gitday",
  "devDependencies": {
    "@types/node": "^20.8.0"
  },
  "bin": {
    "gitday": "./src/index.ts"
  },
  "description": "CLI tool to generate daily progress logs from git commits and diffs",
  "keywords": ["cli", "git", "daily", "logger", "progress"],
  "license": "MIT",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "start": "bun run dist/index.js",
    "publish": "bun publish --access public"
  },
  "dependencies": {
    "@google/genai": "^1.4.0"
  }
}